import { Service } from 'egg';
export declare abstract class BaseAuth extends Service {
    /** 权限名称 */
    static displayName: string;
    /** 判断是否符合角色 */
    abstract has(...args: any[]): Promise<boolean>;
}
export interface RoleMetaInfo {
    /** 需要参数名列表 */
    params: {
        name: string;
        required: boolean;
    }[];
}
export declare function getRoleInfo(roleType: typeof BaseAuth): RoleMetaInfo;
/** opt param for BaseAuth */
export declare function opt(): (target: any, key: any, index: any) => any;
