import { InfoObject, ContactObject, OpenAPIObject } from 'openapi3-ts';
import { RouteType } from '../type';
/** convert routeData to OpenAPI(3.x) json schema */
export declare function convertToOpenAPI(info: {
    base: InfoObject;
    contact?: ContactObject;
}, data: RouteType[]): OpenAPIObject;
