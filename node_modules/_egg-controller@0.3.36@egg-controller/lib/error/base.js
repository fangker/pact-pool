"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
class BaseCustomError extends Error {
    constructor(errorData = '', logData) {
        super(typeof errorData === 'string' ? errorData : errorData.message);
        this.logData = logData;
        const _a = typeof errorData === 'string'
            ? {
                code: -1,
                message: errorData,
            }
            : errorData, { code, message } = _a, other = tslib_1.__rest(_a, ["code", "message"]);
        this.code = code;
        this.errors = other;
        Object.defineProperty(this, 'logData', {
            configurable: true,
            enumerable: false,
        });
    }
    get realStatus() {
        return this.status;
    }
}
exports.BaseCustomError = BaseCustomError;
