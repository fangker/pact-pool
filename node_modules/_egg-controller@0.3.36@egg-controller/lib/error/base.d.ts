export interface ErrorType {
    code: number;
    message: string;
}
export declare abstract class BaseCustomError<ET extends ErrorType = ErrorType> extends Error {
    logData?: any;
    abstract get status(): number;
    get realStatus(): number;
    readonly code: number;
    readonly errors: any;
    constructor(errorData?: string | ET, logData?: any);
}
